import React from 'react';
import ReactMap from '../reactMap/ReactMap';

const Landing = () => {
    return(
        <div>
            <h1>Landing Page</h1>
            <ReactMap />
        </div>
    );
}

export default Landing;